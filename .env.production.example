# Production env for vendorflow-web (Vercel). Set in the Vercel project's
# Environment Variables. NEXT_PUBLIC_ is baked into the browser bundle at build
# time, so a change here requires a redeploy.

# Base URL of the deployed api (Render), no trailing slash.
NEXT_PUBLIC_API_URL=https://vendorflow-api.onrender.com
